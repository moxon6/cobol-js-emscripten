
FROM trzeci/emscripten

RUN apt-get update \
    && apt-get -y install --no-install-recommends \
    vim debootstrap xz-utils lbzip2 lzip clang m4 texinfo g++-multilib \
    autoconf gettext autopoint libtool
